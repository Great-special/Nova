# ── Core voice ────────────────────────────────────────────
SpeechRecognition>=3.10.0
pyaudio>=0.2.13          # mic input (may need: pip install pipwin && pipwin install pyaudio)
pyttsx3>=2.90            # offline TTS fallback
gTTS>=2.3.2              # online TTS primary
pygame>=2.5.0            # audio playback for gTTS

# ── STT: Local Whisper (optional — comment out if not using) ──
openai-whisper>=20231117
soundfile>=0.12.1        # audio format conversion for Whisper
numpy>=1.24.0

# ── AI brain ──────────────────────────────────────────────
# No extra packages needed — uses stdlib urllib for API calls
# (google-generativeai SDK is optional; we use raw REST)

# ── Utilities ─────────────────────────────────────────────
requests>=2.31.0         # (optional, for extended features)
pywhatkit>=5.4
beautifulsoup4>=4.12
pyfiglet>=1.0
google-api-python-client>=2.0
